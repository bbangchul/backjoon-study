#include <bits/stdc++.h>
using namespace std;

void solve()
{
    while (1)
    {
        string a;
        getline(cin, a);
        if (a == ".")
            break;
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
}