#include <bits/stdc++.h>
using namespace std;

void solve()
{
    string p;
    double n;
    deque<int> v(101);
    cin >> p >> n;

    for (int i = 0; i < n; i++)
    {
    }

    double pl = p.length();
    for (int i = 0; i < pl; i++)
    {
        if (p[i] == 'D')
        {
        }
    }
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        solve();
    }
}